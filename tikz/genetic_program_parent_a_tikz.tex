documentclass[tikz, border=2pt]{standalone}
% main document, called main.tex
\usepackage{tikz}
\usetikzlibrary{bayesnet}
\usetikzlibrary{shapes}
\begin{document}

% \title{genetic_program_parent_a}
% \author{Dhruv Patel}
% \date{July 2022}

\tikzstyle{node} = [circle, draw=blue!50, minimum size=10mm, fill = blue!50]
\begin{tikzpicture}
        \node[node](plus){$+$};
        \node[node, below left = of plus](sin){$sin$};
        \node[node, below right = of plus](^){$\land$};
        \node[node, below left = of sin](x){$x$};
        \node[node, below left = of ^](2){$2$};
        \node[node, below right = of ^, draw = red!90, line width = 0.5mm](plus2){$+$};
        \node[node, below left = of plus2](x2){$x$};
        \node[node, below right = of plus2](y){$y$};
        \path[thick]
            (plus) edge(sin) edge(^) 
            (sin) edge(x)
            (^) edge(2) edge(plus2)
            (plus2) edge(x2) edge(y);
	
\end{tikzpicture}
\end{document}
